[tool.poetry]
name = "ibenthos-pt-ifdo-gui"
version = "0.1.0"
description = ""
authors = ["Brendan Do <brendan.do@csiro.au>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pyqt6 = "^6.8.1"
piexif = "^1.1.3"
geotag-pt = {git = "ssh://git@bitbucket.csiro.au:7999/visage/geotag_pt.git", rev = "main"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
